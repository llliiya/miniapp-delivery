databaseChangeLog:
  - changeSet:
      id: 015-balance-transaction-order-completed-unique
      author: delivery
      changes:
        - sql:
            sql: >
              CREATE UNIQUE INDEX uk_balance_tx_order_completed
              ON delivery.balance_transactions (order_id, type)
              WHERE type = 'ORDER_COMPLETED' AND order_id IS NOT NULL
